# Source-Wire Memory Engine Wrapper Runtime PRD

## What to build

Plan a future wrapper runtime implementation unit that keeps `Source-Wire-Memory-Engine` separate while proving a narrow owner-hosted API policy wrapper and MCP adapter path with synthetic fixtures only.

This is a PRD/planning issue only unless the owner separately approves implementation.

## Acceptance criteria

- [ ] The PRD preserves the `WRAP` recommendation.
- [ ] The PRD keeps `Source-Wire-Memory-Engine` separate.
- [ ] The PRD forbids copying AGPLv3 code into Source-Wire.
- [ ] The PRD requires MCP to call Source-Wire API policy.
- [ ] The PRD requires no-auto-promotion.
- [ ] The PRD requires synthetic fixtures only.
- [ ] The PRD blocks real data, deployment, npm publishing, GitHub release creation, managed hosting, and contribution acceptance.
- [ ] The PRD is sliced into implementation-ready local issue drafts.

## Blocked by

- Exact owner approval before public issue publication or implementation.
