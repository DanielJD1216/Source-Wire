# Daily Workflow Fixture Matrix

This fixture is synthetic.

It models the public Source-Wire daily owner memory workflow without real user data, private paths, provider exports, live connectors, database access, API runtime, MCP runtime, or automatic trusted memory promotion.

Use Node.js 22 with npm from the repository root. For the complete local setup path, read the [Quickstart](../../../docs/quickstart.md).

The fixture is smoke-validated by:

```bash
npm run daily-workflow:smoke
```
